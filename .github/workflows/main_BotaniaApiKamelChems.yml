# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - BotaniaApiKamelChems

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://botaniaapikamelchems.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_2001c21bf8924dee854de1ae3f7c301e }}
        password: ${{ secrets.AzureAppService_ContainerPassword_0a25e5fe974843f98035280d25695fb4 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: botaniaapikamelchems.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_2001c21bf8924dee854de1ae3f7c301e }}/botania_api:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'BotaniaApiKamelChems'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_bee62e571779421390bbc59c7054b975 }}
        images: 'botaniaapikamelchems.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_2001c21bf8924dee854de1ae3f7c301e }}/botania_api:${{ github.sha }}'